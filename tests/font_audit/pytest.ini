[pytest]
# このディレクトリ単体で pytest を回す前提の設定。
# - conftest.py が `from sources import ...` で同ディレクトリのパーサを読むため、
#   rootdir(= このディレクトリ)を sys.path に載せる。
# - テスト検出はこのディレクトリ配下の test_*.py に限定。
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra
